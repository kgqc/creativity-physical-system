# 前端测试清单

## 构建与命令

- [x] `npm install` 成功
- [x] `npm run dev` 成功启动并可访问
- [x] `npm run build` 成功
- [x] `npm run preview` 成功启动，页面与 Mock MP4 均返回 HTTP 200
- [x] `npm run lint` 无错误

## Mock 完整流程

- [x] 输入 Participant Code 并创建/恢复 Session
- [ ] 上传图片或视频并显示本地预览（浏览器自动化接口不支持设置文件输入；已通过类型、大小和 Object URL 代码检查）
- [x] Initial Motion 经历准备、排队、生成、处理和成功
- [x] Text Edit 创建子版本且保留旧版本
- [x] Motion Edit 提交规范化 Gesture
- [x] Combined Edit 同时提交文字与 Gesture
- [x] `VITE_MOCK_FORCE_FAILURE=true` 时显示固定失败、错误码和重试
- [x] 取消活动任务后显示 `CANCELLED`
- [x] 相同任务重复轮询只创建一个版本
- [x] 页面刷新后 Session、任务和版本仍存在
- [ ] 动画下载可触发（Mock 视频 URL 与按钮可用，浏览器自动化未暴露合成下载事件）
- [x] 项目 JSON 导出按钮执行，导出结构经代码检查不包含密钥、Cookie 或内部后端字段

## 真实 API 切换

- [x] `VITE_USE_MOCK_API=false` 后业务调用选择 Real API，不读取 Mock Store
- [x] 所有真实请求通过 `VITE_API_BASE_URL`
- [x] 上传请求由浏览器生成 multipart boundary
- [x] Cookie 请求保留 `credentials: include`
- [x] 组件中没有固定后端 URL 或直接 Mock 数据引用
- [x] 相对结果 URL使用 API 根地址解析

真实后端尚未由合作者提供，因此真实 API 的网络联调未执行。

## 安全检查

- [x] 源码无 RunningHub API Key
- [x] 源码无 Workflow Secret
- [x] 源码无 Webhook Secret
- [x] 源码无数据库或 Session Secret
- [x] `dist/` 无上述敏感变量名或值
- [x] Git 忽略 `.env.local`，仓库中无真实研究数据

测试完成后应在本文件中更新勾选项；环境依赖导致无法执行的项目需要写明原因。

## 依赖审计

`npm audit` 仍报告 1 个低危问题：Vite 当前依赖的 esbuild 版本在 Windows 开发服务器场景存在任意文件读取风险。`npm audit fix` 已执行但没有可应用的兼容更新。本次验证环境为 macOS，生产构建不运行 Vite 开发服务器；后续依赖升级时应重新审计。
